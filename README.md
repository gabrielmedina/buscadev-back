## DevSearch API

> API with Node.js to dev search

In the project directory, you can run:

`npm start`

localhost:3333

## API

| Verb | URI Pattern |
|---|---|---|
| GET | /devs |
| POST | /devs |
| GET | /search |